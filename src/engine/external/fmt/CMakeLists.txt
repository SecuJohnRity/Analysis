# CMakeLists.txt for fmt (source build)
# This file is a placeholder.
# fmt has its own CMake build system.

cmake_minimum_required(VERSION 3.22.1)
project(wazuh_ext_fmt NONE)

message(STATUS "This CMakeLists.txt (src/engine/external/fmt/CMakeLists.txt) is a placeholder. fmt's own CMake script is used directly.")
